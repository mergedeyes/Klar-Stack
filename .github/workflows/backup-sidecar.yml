name: Build and deploy backup sidecar
on:
  push:
    branches: ["main"]
    paths:
      - "deploy/postgres-backup/**"
      - ".github/workflows/backup-sidecar.yaml"

permissions:
  contents: read
  packages: write

jobs:
  build:
    name: Build and deploy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: docker login
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: docker build and push
        uses: docker/build-push-action@v5
        with:
          context: ./deploy/postgres-backup
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/klar-db-backup:${{ github.sha }}

      - name: Update container image on Magic Containers
        if: vars.APP_ID != ''
        uses: BunnyWay/actions/container-update-image@main
        with:
          app_id: ${{ vars.APP_ID }}
          api_key: ${{ secrets.BUNNYNET_API_KEY }}
          container: db-backup
          image_tag: "${{ github.sha }}"
